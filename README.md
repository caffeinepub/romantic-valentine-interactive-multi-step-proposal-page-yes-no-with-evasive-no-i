## Romantic Valentine interactive multi-step proposal page (Yes/No with evasive No, image, shayari, songs, quotes, date ideas)

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
